1. Install the project requirements using the following command while in the current directory in a python 3.7+ environment:
	python -m pip install -r "requirements.txt"

2. Use the following command to run the Flask server locally:
	python run_app/py

NOTE: You can change the port being used in the 'run_app.py' file.